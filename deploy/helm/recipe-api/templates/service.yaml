apiVersion: v1
kind: Service
metadata:
  name: {{ include "recipe-api.fullname" . }}
  labels:
    {{- include "recipe-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "recipe-api.selectorLabels" . | nindent 4 }}
